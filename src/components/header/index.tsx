import styles from './../../styles/page.module.css';

const Header = () => {
    return (
        <header className={styles.header}>
            <h1>Weather</h1>
        </header>
    )
}

export default Header;